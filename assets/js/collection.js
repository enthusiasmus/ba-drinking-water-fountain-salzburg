var MarkerCollection = Backbone.Collection.extend({
	initialize: function(){
	},
	model: MarkerModel
});

var FeedItemCollection = Backbone.Collection.extend({
  initialize: function() {
  },
  model: FeedItemModel
});