var InfoView = Backbone.View.extend({
  el: $('#info'),
  initialize: function() {
    this.render();
  },
  render: function() {
    // var template = _.template( $('#info_template').html());
    // $(this.el).html(template);
  } 
});